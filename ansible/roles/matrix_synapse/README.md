# matrix_synapse

Placeholder role delivering Matrix Synapse with PostgreSQL backing, Caddy ingress integration, and optional bot bootstrap. Extend `tasks/main.yml` with concrete handlers once package selection is final.
